
Cоздаёт какие-то непонятные файлы если запустить на тестах из тестового репозитория.

Я не понял почему именно, после чтения commit-ов, но проблема решилась.

Есть проблема с именами переменных _ тоже является нормальным символом, который может входить в имя переменной.

исправлено, но в одном из тестов есть конструкция "znach: polzovatel_$UID_faylo"

и по echo $LS пишет что-то несусветное. под valgrind не запускал пока, но думаю ошибки с хождением по памяти там есть.  

локально ничего не пишет
